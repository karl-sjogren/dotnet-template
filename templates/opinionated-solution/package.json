{
  "name": "scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "push-release": "git checkout main && git push && git checkout develop && git push && git push --tags --no-verify"
  }
}
